// test of string utils features

use stdio;
use stringutils;

var string1 = $string[input("Please input a string: ")];
var string2 = $string[input("Please input a second string: ")];

var number1 = $number[input("Please input a number: ")];
var number2 = $number[input("Please input a second number: ")];

printf("\nWatch me join %s and %s together using pure magic!\n", string1, string2);
printf($string[join(string1, string2)]);